extend ../layout

block content
	.container
		.row
			form.form-horizontal(method="post", action="/admin/category" style='margin-bottom: 60px;')
				.form-group
					label.col-sm-3.control-label(for="inputCategory") 电影分类
					.col-sm-9
						input#inputCategory.form-control(type="text", name='category[name]', value=category.name)
						button.btn.btn-default(type="submit",style="margin-top: 15px;") 录入
				//.form-group
				//	.col-sm-offset-3.col-sm-9
				//	button.btn.btn-default(type="submit") 录入